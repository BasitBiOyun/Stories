name: Temporary Actions smoke test

on:
  push:
    branches: [preview]
    paths:
      - .github/workflows/temporary-actions-smoke-test.yml

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "actions-runner-ok"
